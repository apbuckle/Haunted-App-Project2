<h1>All Attractions <a href='/attractions/new'>New</a></h1> 

<div>
    {{#each attractions}}
        <div>name: {{this.name}} <a href='/attractions/{{this._id}}'>Go to Attrcation</a> </div>
        <div>description: {{this.description}}</div>
        <div>price: {{this.price}}</div>
        <br>
    {{/each}}

</div>