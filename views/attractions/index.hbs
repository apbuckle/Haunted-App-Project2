<h1>Attractions at {{venue.name}}</h1>

<div>
    {{#each venue.attractions}}
        <br>
        <div> Enter <a href='/venues/{{../venue._id}}/attractions/{{this._id}}'class="effect">{{this.name}}</a></div>
        <br>
        <br>
    {{/each}}
<br>
<div> <a href="/venues/{{venue._id}}/attractions/new"class="effect">New</a> </div>
<br>
<div> <a href='/venues/{{venue._id}}'class="effect">Return to Venue Page</a> </div>

</div>